exclude:
  - jekyll.*.sh
  - LICENSE
  - old

defaults:
  - scope:
      path: games/*/*
    values:
      layout: game
  - scope:
      path: ""
    values:
      layout: default

theme: just-the-docs

# plugins:
#   - jekyll-feed
#   - jekyll-seo-tag
#   - jekyll-sitemap
